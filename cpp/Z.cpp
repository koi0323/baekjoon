//
// Created by jkil on 25. 10. 27..
//
#include <iostream>
#include <vector>
#include <cmath>

using namespace std;

int main(void) {
    int n, r, c;
    cin >> n >> r >> c;
    int cen_r, cen_c;
    while (n > 1) {
        cen_r = pow(2, n) / 2;
        cen_c = pow(2, n) / 2;

    }
}